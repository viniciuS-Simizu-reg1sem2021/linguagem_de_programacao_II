# String ->
    # \n (New Line = <br>)
    # \r (Retorna para a primeira caractere da string, sobrepondo-a)
    # \t (Tab)
    # \b (Backspace, volta uma caractere e sobrepõe ela)
    # \\ (String da \)
    # \x (Inserir um caractere pelo seu número hexadecimal)

# String - Índices ->
    # string[índice]
    # a = "abcde"
    # a[0] = "a"
    # a[2] = "c"
    # a[-1] = "e"

# String - Fatias (slices)
    # a = "abcde"
    # a[2:] = "cde"
    # a[:] = "abcde"
    # a[-1:] = "e"
    # a[:-1] = "abcd"


# Expressões booleanas - True or False
    # Relacionais: >, ==, <, <=, >=
    # Booleanos: and, or, not
    # Ordem de equação: relacionais > not > and > or


# Funções Embutidas
    # abs(x) - Retorna o balor absoluto do número x
        # abs(10) -> 10
        # abs(-19) -> 19

    # chr(x) - Retorna uma string com um único caractere cujo código ASCII é x
        # chr(95) -> '_'
        # chr(99) -> 'c'

    # ord(s) - Retorna o código ASCII do caractere s
        # ord('a') -> 97


# Importando módulos
    # import <módulo>
        # import math
        # a = math.sin(30)
    # from <módulo> import <nome, ..., nome>
    # from <módulo> import *






